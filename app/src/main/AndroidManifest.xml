<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.harshit.pokdex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".actiivty.MainActivity"
            android:launchMode="singleInstance"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name=".actiivty.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:launchMode="singleInstance"
            android:parentActivityName=".actiivty.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="app.harshit.pokdex.actiivty.MainActivity" />
        </activity>
        <activity
            android:name=".actiivty.IntroActivity"
            android:launchMode="singleInstance"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name=".actiivty.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>